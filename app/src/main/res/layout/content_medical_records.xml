<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_medical_records"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.mycompany.kittylogs.MedicalRecordsActivity"
    tools:showIn="@layout/activity_medical_records"
    android:orientation="vertical">

    <Button
        android:text="Medications"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/meds_button"
        android:onClick="startMedsActivity" />

    <Button
        android:text="Vet visits"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/vet_visits_button"
        android:onClick="startVetVisitsActivity" />

    <Button
        android:text="Vets"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/vets_button"
        android:onClick="startVetsActivity" />

</LinearLayout>
